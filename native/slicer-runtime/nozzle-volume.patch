diff --git a/app/src/main/cpp/include/sapil.h b/app/src/main/cpp/include/sapil.h
--- a/app/src/main/cpp/include/sapil.h
+++ b/app/src/main/cpp/include/sapil.h
@@ -517,6 +517,7 @@ struct SliceConfig {
     int nozzle_material = 0;
     int nozzle_hrc = 0;
     float nozzle_height = 2.5f;
+    float nozzle_volume = 0.0f;
     std::vector<float> minimum_layer_heights;
     std::vector<float> maximum_layer_heights;
     std::vector<float> extruder_offsets_x;
diff --git a/app/src/main/cpp/src/sapil_config.cpp b/app/src/main/cpp/src/sapil_config.cpp
--- a/app/src/main/cpp/src/sapil_config.cpp
+++ b/app/src/main/cpp/src/sapil_config.cpp
@@ -551,6 +551,7 @@ SliceConfig sliceConfigFromJava(JNIEnv* env, jobject jconfig) {
     config.nozzle_material = getInt("nozzleMaterial");
     config.nozzle_hrc = getInt("nozzleHrc");
     config.nozzle_height = getFloat("nozzleHeight");
+    config.nozzle_volume = getFloat("nozzleVolume");
     config.minimum_layer_heights = getFloatArray("minimumLayerHeights");
     config.maximum_layer_heights = getFloatArray("maximumLayerHeights");
     config.extruder_offsets_x = getFloatArray("extruderOffsetsX");
@@ -1172,6 +1173,7 @@ void updateJavaSliceConfig(JNIEnv* env, jobject jconfig, const SliceConfig& confi
     setInt("nozzleMaterial", config.nozzle_material);
     setInt("nozzleHrc", config.nozzle_hrc);
     setFloat("nozzleHeight", config.nozzle_height);
+    setFloat("nozzleVolume", config.nozzle_volume);
     setStringArray("filamentStartGcodes", config.filament_start_gcodes);
     setStringArray("filamentEndGcodes", config.filament_end_gcodes);
     setIntArray("filamentAdaptivePressureAdvance", config.filament_adaptive_pressure_advance);
diff --git a/app/src/main/cpp/src/sapil_print.cpp b/app/src/main/cpp/src/sapil_print.cpp
--- a/app/src/main/cpp/src/sapil_print.cpp
+++ b/app/src/main/cpp/src/sapil_print.cpp
@@ -570,6 +570,11 @@ static void applyConfigToPrusa(const SliceConfig& config, Slic3r::DynamicPrintCo
         throw std::invalid_argument("Invalid nozzle height");
     }
     dpc.set_key_value("nozzle_height", new Slic3r::ConfigOptionFloat(config.nozzle_height));
+    if (!std::isfinite(config.nozzle_volume) ||
+        config.nozzle_volume < 0.0f || config.nozzle_volume > 1000.0f) {
+        throw std::invalid_argument("Invalid nozzle volume");
+    }
+    dpc.set_key_value("nozzle_volume", new Slic3r::ConfigOptionFloat(config.nozzle_volume));
     dpc.set_key_value("machine_max_acceleration_extruding", machine_modes(max_acceleration_extruding));
     dpc.set_key_value("machine_max_acceleration_retracting", machine_modes(positive(config.machine_max_acceleration_retracting, 5000.0)));
     dpc.set_key_value("machine_max_acceleration_travel", machine_modes(max_acceleration_travel));
@@ -2116,6 +2121,7 @@ SliceResult SlicerEngine::slice(const SliceConfig& config, ProgressCallback progr
                     "nozzle_type",
                     "nozzle_hrc",
                     "nozzle_height",
+                    "nozzle_volume",
                     "min_layer_height",
                     "max_layer_height",
                     // M204 acceleration values (used by GCodeProcessor for time estimation
