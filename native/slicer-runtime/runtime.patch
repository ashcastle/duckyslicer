diff --git a/app/src/main/cpp/CMakeLists.txt b/app/src/main/cpp/CMakeLists.txt
index 963808df..2f2043a3 100644
--- a/app/src/main/cpp/CMakeLists.txt
+++ b/app/src/main/cpp/CMakeLists.txt
@@ -486,16 +486,29 @@ endif()
 
 # LibBGCode — stub (binary G-code not needed)
 include_directories(${CMAKE_CURRENT_SOURCE_DIR}/extern/libbgcode_stub/include)
-include_directories(${CMAKE_CURRENT_SOURCE_DIR}/extern/jpeg_stub/include)
 add_library(bgcode_convert INTERFACE)
 add_library(LibBGCode::bgcode_convert ALIAS bgcode_convert)
 
-# NLopt — stub
-add_library(NLopt INTERFACE)
+# Real optimization and JPEG implementations are required. Silent stubbed
+# output is not acceptable in a production slicer runtime.
+set(NLOPT_ROOT "${EXTERN_DIR}/nlopt")
+add_library(NLopt STATIC IMPORTED)
+set_target_properties(NLopt PROPERTIES
+    IMPORTED_LOCATION "${NLOPT_ROOT}/lib/libnlopt.a"
+    INTERFACE_INCLUDE_DIRECTORIES "${NLOPT_ROOT}/include"
+)
+
+set(JPEG_ROOT "${EXTERN_DIR}/jpeg")
+include_directories(BEFORE "${JPEG_ROOT}/include")
+add_library(jpeg STATIC IMPORTED)
+set_target_properties(jpeg PROPERTIES
+    IMPORTED_LOCATION "${JPEG_ROOT}/lib/libjpeg.a"
+    INTERFACE_INCLUDE_DIRECTORIES "${JPEG_ROOT}/include"
+)
 
-# PNG/JPEG — stubs (not needed for headless slicing)
+# PNGReadWrite is excluded from the headless source list. The remaining PNG
+# include is unused and supplied by a declaration-free compatibility header.
 add_library(png_stub INTERFACE)
-add_library(jpeg_stub INTERFACE)
 
 if(SLICER_BACKEND STREQUAL "orca")
     # OpenCV — stub (image analysis features not needed on Android)
@@ -1075,6 +1088,8 @@ if(SLICER_BACKEND STREQUAL "orca")
         Clipper2
         z
         expat
+        NLopt
+        jpeg
     )
 else()
     target_link_libraries(libslic3r PRIVATE
diff --git a/app/src/main/cpp/include/sapil.h b/app/src/main/cpp/include/sapil.h
index 116a4a85..bf283375 100644
--- a/app/src/main/cpp/include/sapil.h
+++ b/app/src/main/cpp/include/sapil.h
@@ -40,14 +40,51 @@ struct SliceConfig {
     int perimeters = 2;
     int top_solid_layers = 5;
     int bottom_solid_layers = 4;
+    float top_shell_thickness = 0.0f;
+    float bottom_shell_thickness = 0.0f;
     float fill_density = 0.15f;  // 0.0 - 1.0
     std::string fill_pattern = "gyroid";
+    std::string top_surface_pattern = "monotonicline";
+    std::string bottom_surface_pattern = "monotonic";
+    std::string internal_solid_infill_pattern = "monotonic";
 
     // Speed settings (mm/s)
     float print_speed = 60.0f;
+    float inner_wall_speed = 90.0f;
+    float sparse_infill_speed = 81.0f;
+    float internal_solid_infill_speed = 75.0f;
+    float top_surface_speed = 60.0f;
+    float support_speed = 100.0f;
+    float bridge_speed = 50.0f;
+    float gap_infill_speed = 75.0f;
+    float first_layer_infill_speed = 42.0f;
+    float support_interface_speed = 80.0f;
+    bool overhang_speed_enabled = true;
+    float overhang_speed_1 = 55.0f;
+    bool overhang_speed_1_percent = false;
+    float overhang_speed_2 = 30.0f;
+    bool overhang_speed_2_percent = false;
+    float overhang_speed_3 = 10.0f;
+    bool overhang_speed_3_percent = false;
+    float overhang_speed_4 = 10.0f;
+    bool overhang_speed_4_percent = false;
     float travel_speed = 150.0f;
     float first_layer_speed = 20.0f;
 
+    // Feature flow ratios.
+    float bridge_flow_ratio = 1.0f;
+    float internal_bridge_flow_ratio = 1.0f;
+    float top_surface_flow_ratio = 1.0f;
+    float bottom_surface_flow_ratio = 1.0f;
+
+    // Feature acceleration (mm/s²). Zero keeps Orca's inheritance behavior.
+    float default_acceleration = 0.0f;
+    float outer_wall_acceleration = 0.0f;
+    float inner_wall_acceleration = 0.0f;
+    float top_surface_acceleration = 0.0f;
+    float travel_acceleration = 0.0f;
+    float first_layer_acceleration = 0.0f;
+
     // Temperature
     int nozzle_temp = 210;
     int bed_temp = 60;
@@ -60,6 +97,16 @@ struct SliceConfig {
     bool support_enabled = false;
     std::string support_type = "normal"; // "normal", "tree"
     float support_angle = 45.0f;
+    int support_interface_top_layers = 3;
+    int support_interface_bottom_layers = 0;
+    float support_interface_spacing = 0.5f;
+    float support_bottom_interface_spacing = 0.5f;
+    float support_top_z_distance = 0.2f;
+    float support_bottom_z_distance = 0.2f;
+    float support_object_xy_distance = 0.35f;
+    std::string support_base_pattern = "default";
+    std::string support_interface_pattern = "auto";
+    std::string support_style = "default";
     int support_filament = 0;            // 1-based Orca filament index; 0 = default
     int support_interface_filament = 0;  // 1-based Orca filament index; 0 = default
 
@@ -68,6 +115,28 @@ struct SliceConfig {
     float skirt_distance = 6.0f;
     float brim_width = 0.0f;
 
+    // Orca wall semantics. Keep exterior and interior paths independently
+    // configurable instead of reducing a process profile to wall count alone.
+    float outer_wall_line_width = 0.42f;
+    float inner_wall_line_width = 0.45f;
+    float top_surface_line_width = 0.42f;
+    float sparse_infill_line_width = 0.45f;
+    float internal_solid_infill_line_width = 0.45f;
+    float support_line_width = 0.42f;
+    float initial_layer_line_width = 0.5f;
+    std::string seam_position = "aligned";
+    std::string ironing_type = "no ironing";
+    std::string ironing_pattern = "rectilinear";
+    float ironing_flow = 10.0f;
+    float ironing_spacing = 0.1f;
+    float ironing_speed = 20.0f;
+    std::string wall_generator = "arachne";
+    std::string wall_sequence = "inner-outer";
+    bool detect_thin_walls = false;
+    bool detect_overhang_walls = true;
+    bool only_one_wall_on_top = false;
+    bool precise_outer_walls = true;
+
     // Printer bed (Snapmaker U1: 270x270x270mm)
     float bed_size_x = 270.0f;
     float bed_size_y = 270.0f;
@@ -98,6 +167,22 @@ struct SliceConfig {
     // template expressions at G-code generation time.
     std::string machine_start_gcode;
     std::string machine_end_gcode;
+    std::string gcode_flavor = "marlin";
+    float machine_max_speed_x = 500.0f;
+    float machine_max_speed_y = 500.0f;
+    float machine_max_speed_z = 20.0f;
+    float machine_max_speed_e = 30.0f;
+    float machine_max_acceleration_x = 20000.0f;
+    float machine_max_acceleration_y = 20000.0f;
+    float machine_max_acceleration_z = 500.0f;
+    float machine_max_acceleration_e = 5000.0f;
+    float machine_max_acceleration_extruding = 20000.0f;
+    float machine_max_acceleration_retracting = 5000.0f;
+    float machine_max_acceleration_travel = 20000.0f;
+    float machine_max_jerk_x = 9.0f;
+    float machine_max_jerk_y = 9.0f;
+    float machine_max_jerk_z = 3.0f;
+    float machine_max_jerk_e = 2.5f;
 
     // F91 (2026-05-25): per-extruder filament tuning sourced from the user's filament
     // library on the Kotlin side at slice time. Empty vector = "user has not set this
diff --git a/app/src/main/cpp/src/orca_stubs.cpp b/app/src/main/cpp/src/orca_stubs.cpp
index 51a2f8fb..f5cdd410 100644
--- a/app/src/main/cpp/src/orca_stubs.cpp
+++ b/app/src/main/cpp/src/orca_stubs.cpp
@@ -5,6 +5,7 @@
 // Include path has extern/libslic3r_android first, so "Format/STEP.hpp" picks
 // up our stub before the real OCCT-dependent header.
 #include "Format/STEP.hpp"
+#include "Polygon.hpp"
 #include <string>
 #include <vector>
 #include <functional>
@@ -12,7 +13,6 @@
 namespace Slic3r {
 class PerimeterGenerator;
 namespace Arachne { struct ExtrusionLine; }
-class Polygon;
 
 // STEP — excluded (needs OCCT). Implement Step class methods declared in stub header.
 Step::Step(std::string, ImportStepProgressFn, StepIsUtf8Fn) {}
@@ -28,7 +28,7 @@ bool load_step(const char*, Model*, bool&,
 // FuzzySkin — excluded (needs libnoise)
 namespace Feature { namespace FuzzySkin {
 void apply_fuzzy_skin(Arachne::ExtrusionLine*, const PerimeterGenerator&, bool) {}
-void apply_fuzzy_skin(const Polygon&, const PerimeterGenerator&, unsigned long, bool) {}
+Polygon apply_fuzzy_skin(const Polygon& polygon, const PerimeterGenerator&, size_t, bool) { return polygon; }
 void group_region_by_fuzzify(PerimeterGenerator&) {}
 } } // namespace Feature::FuzzySkin
 
diff --git a/app/src/main/cpp/src/sapil_config.cpp b/app/src/main/cpp/src/sapil_config.cpp
index d93efb7d..dab7aac7 100644
--- a/app/src/main/cpp/src/sapil_config.cpp
+++ b/app/src/main/cpp/src/sapil_config.cpp
@@ -92,10 +92,43 @@ SliceConfig configFromJava(JNIEnv* env, jobject jconfig) {
     config.perimeters = getInt("perimeters");
     config.top_solid_layers = getInt("topSolidLayers");
     config.bottom_solid_layers = getInt("bottomSolidLayers");
+    config.top_shell_thickness = getFloat("topShellThickness");
+    config.bottom_shell_thickness = getFloat("bottomShellThickness");
     config.fill_density = getFloat("fillDensity");
     config.fill_pattern = getString("fillPattern");
+    config.top_surface_pattern = getString("topSurfacePattern");
+    config.bottom_surface_pattern = getString("bottomSurfacePattern");
+    config.internal_solid_infill_pattern = getString("internalSolidInfillPattern");
 
     config.print_speed = getFloat("printSpeed");
+    config.inner_wall_speed = getFloat("innerWallSpeed");
+    config.sparse_infill_speed = getFloat("sparseInfillSpeed");
+    config.internal_solid_infill_speed = getFloat("internalSolidInfillSpeed");
+    config.top_surface_speed = getFloat("topSurfaceSpeed");
+    config.support_speed = getFloat("supportSpeed");
+    config.bridge_speed = getFloat("bridgeSpeed");
+    config.gap_infill_speed = getFloat("gapInfillSpeed");
+    config.first_layer_infill_speed = getFloat("firstLayerInfillSpeed");
+    config.support_interface_speed = getFloat("supportInterfaceSpeed");
+    config.overhang_speed_enabled = getBool("overhangSpeedEnabled");
+    config.overhang_speed_1 = getFloat("overhangSpeed1");
+    config.overhang_speed_1_percent = getBool("overhangSpeed1Percent");
+    config.overhang_speed_2 = getFloat("overhangSpeed2");
+    config.overhang_speed_2_percent = getBool("overhangSpeed2Percent");
+    config.overhang_speed_3 = getFloat("overhangSpeed3");
+    config.overhang_speed_3_percent = getBool("overhangSpeed3Percent");
+    config.overhang_speed_4 = getFloat("overhangSpeed4");
+    config.overhang_speed_4_percent = getBool("overhangSpeed4Percent");
+    config.bridge_flow_ratio = getFloat("bridgeFlowRatio");
+    config.internal_bridge_flow_ratio = getFloat("internalBridgeFlowRatio");
+    config.top_surface_flow_ratio = getFloat("topSurfaceFlowRatio");
+    config.bottom_surface_flow_ratio = getFloat("bottomSurfaceFlowRatio");
+    config.default_acceleration = getFloat("defaultAcceleration");
+    config.outer_wall_acceleration = getFloat("outerWallAcceleration");
+    config.inner_wall_acceleration = getFloat("innerWallAcceleration");
+    config.top_surface_acceleration = getFloat("topSurfaceAcceleration");
+    config.travel_acceleration = getFloat("travelAcceleration");
+    config.first_layer_acceleration = getFloat("firstLayerAcceleration");
     config.travel_speed = getFloat("travelSpeed");
     config.first_layer_speed = getFloat("firstLayerSpeed");
 
@@ -108,12 +141,41 @@ SliceConfig configFromJava(JNIEnv* env, jobject jconfig) {
     config.support_enabled = getBool("supportEnabled");
     config.support_type = getString("supportType");
     config.support_angle = getFloat("supportAngle");
+    config.support_interface_top_layers = getInt("supportInterfaceTopLayers");
+    config.support_interface_bottom_layers = getInt("supportInterfaceBottomLayers");
+    config.support_interface_spacing = getFloat("supportInterfaceSpacing");
+    config.support_bottom_interface_spacing = getFloat("supportBottomInterfaceSpacing");
+    config.support_top_z_distance = getFloat("supportTopZDistance");
+    config.support_bottom_z_distance = getFloat("supportBottomZDistance");
+    config.support_object_xy_distance = getFloat("supportObjectXYDistance");
+    config.support_base_pattern = getString("supportBasePattern");
+    config.support_interface_pattern = getString("supportInterfacePattern");
+    config.support_style = getString("supportStyle");
     config.support_filament = getInt("supportFilament");
     config.support_interface_filament = getInt("supportInterfaceFilament");
 
     config.skirt_loops = getInt("skirtLoops");
     config.skirt_distance = getFloat("skirtDistance");
     config.brim_width = getFloat("brimWidth");
+    config.outer_wall_line_width = getFloat("outerWallLineWidth");
+    config.inner_wall_line_width = getFloat("innerWallLineWidth");
+    config.top_surface_line_width = getFloat("topSurfaceLineWidth");
+    config.sparse_infill_line_width = getFloat("sparseInfillLineWidth");
+    config.internal_solid_infill_line_width = getFloat("internalSolidInfillLineWidth");
+    config.support_line_width = getFloat("supportLineWidth");
+    config.initial_layer_line_width = getFloat("initialLayerLineWidth");
+    config.seam_position = getString("seamPosition");
+    config.ironing_type = getString("ironingType");
+    config.ironing_pattern = getString("ironingPattern");
+    config.ironing_flow = getFloat("ironingFlow");
+    config.ironing_spacing = getFloat("ironingSpacing");
+    config.ironing_speed = getFloat("ironingSpeed");
+    config.wall_generator = getString("wallGenerator");
+    config.wall_sequence = getString("wallSequence");
+    config.detect_thin_walls = getBool("detectThinWalls");
+    config.detect_overhang_walls = getBool("detectOverhangWalls");
+    config.only_one_wall_on_top = getBool("onlyOneWallOnTop");
+    config.precise_outer_walls = getBool("preciseOuterWalls");
 
     config.bed_size_x = getFloat("bedSizeX");
     config.bed_size_y = getFloat("bedSizeY");
@@ -137,6 +199,22 @@ SliceConfig configFromJava(JNIEnv* env, jobject jconfig) {
     // B106: machine G-code templates for STL files (no embedded Snapmaker profile).
     config.machine_start_gcode = getString("machineStartGcode");
     config.machine_end_gcode = getString("machineEndGcode");
+    config.gcode_flavor = getString("gcodeFlavor");
+    config.machine_max_speed_x = getFloat("machineMaxSpeedX");
+    config.machine_max_speed_y = getFloat("machineMaxSpeedY");
+    config.machine_max_speed_z = getFloat("machineMaxSpeedZ");
+    config.machine_max_speed_e = getFloat("machineMaxSpeedE");
+    config.machine_max_acceleration_x = getFloat("machineMaxAccelerationX");
+    config.machine_max_acceleration_y = getFloat("machineMaxAccelerationY");
+    config.machine_max_acceleration_z = getFloat("machineMaxAccelerationZ");
+    config.machine_max_acceleration_e = getFloat("machineMaxAccelerationE");
+    config.machine_max_acceleration_extruding = getFloat("machineMaxAccelerationExtruding");
+    config.machine_max_acceleration_retracting = getFloat("machineMaxAccelerationRetracting");
+    config.machine_max_acceleration_travel = getFloat("machineMaxAccelerationTravel");
+    config.machine_max_jerk_x = getFloat("machineMaxJerkX");
+    config.machine_max_jerk_y = getFloat("machineMaxJerkY");
+    config.machine_max_jerk_z = getFloat("machineMaxJerkZ");
+    config.machine_max_jerk_e = getFloat("machineMaxJerkE");
 
     // F91: per-extruder filament tuning from the user's filament library.
     config.filament_flow_ratios = getFloatArray("filamentFlowRatios");
@@ -235,9 +313,42 @@ jobject configToJava(JNIEnv* env, const SliceConfig& config) {
     setInt("perimeters", config.perimeters);
     setInt("topSolidLayers", config.top_solid_layers);
     setInt("bottomSolidLayers", config.bottom_solid_layers);
+    setFloat("topShellThickness", config.top_shell_thickness);
+    setFloat("bottomShellThickness", config.bottom_shell_thickness);
     setFloat("fillDensity", config.fill_density);
     setString("fillPattern", config.fill_pattern);
+    setString("topSurfacePattern", config.top_surface_pattern);
+    setString("bottomSurfacePattern", config.bottom_surface_pattern);
+    setString("internalSolidInfillPattern", config.internal_solid_infill_pattern);
     setFloat("printSpeed", config.print_speed);
+    setFloat("innerWallSpeed", config.inner_wall_speed);
+    setFloat("sparseInfillSpeed", config.sparse_infill_speed);
+    setFloat("internalSolidInfillSpeed", config.internal_solid_infill_speed);
+    setFloat("topSurfaceSpeed", config.top_surface_speed);
+    setFloat("supportSpeed", config.support_speed);
+    setFloat("bridgeSpeed", config.bridge_speed);
+    setFloat("gapInfillSpeed", config.gap_infill_speed);
+    setFloat("firstLayerInfillSpeed", config.first_layer_infill_speed);
+    setFloat("supportInterfaceSpeed", config.support_interface_speed);
+    setBool("overhangSpeedEnabled", config.overhang_speed_enabled);
+    setFloat("overhangSpeed1", config.overhang_speed_1);
+    setBool("overhangSpeed1Percent", config.overhang_speed_1_percent);
+    setFloat("overhangSpeed2", config.overhang_speed_2);
+    setBool("overhangSpeed2Percent", config.overhang_speed_2_percent);
+    setFloat("overhangSpeed3", config.overhang_speed_3);
+    setBool("overhangSpeed3Percent", config.overhang_speed_3_percent);
+    setFloat("overhangSpeed4", config.overhang_speed_4);
+    setBool("overhangSpeed4Percent", config.overhang_speed_4_percent);
+    setFloat("bridgeFlowRatio", config.bridge_flow_ratio);
+    setFloat("internalBridgeFlowRatio", config.internal_bridge_flow_ratio);
+    setFloat("topSurfaceFlowRatio", config.top_surface_flow_ratio);
+    setFloat("bottomSurfaceFlowRatio", config.bottom_surface_flow_ratio);
+    setFloat("defaultAcceleration", config.default_acceleration);
+    setFloat("outerWallAcceleration", config.outer_wall_acceleration);
+    setFloat("innerWallAcceleration", config.inner_wall_acceleration);
+    setFloat("topSurfaceAcceleration", config.top_surface_acceleration);
+    setFloat("travelAcceleration", config.travel_acceleration);
+    setFloat("firstLayerAcceleration", config.first_layer_acceleration);
     setFloat("travelSpeed", config.travel_speed);
     setFloat("firstLayerSpeed", config.first_layer_speed);
     setInt("nozzleTemp", config.nozzle_temp);
@@ -247,11 +358,40 @@ jobject configToJava(JNIEnv* env, const SliceConfig& config) {
     setBool("supportEnabled", config.support_enabled);
     setString("supportType", config.support_type);
     setFloat("supportAngle", config.support_angle);
+    setInt("supportInterfaceTopLayers", config.support_interface_top_layers);
+    setInt("supportInterfaceBottomLayers", config.support_interface_bottom_layers);
+    setFloat("supportInterfaceSpacing", config.support_interface_spacing);
+    setFloat("supportBottomInterfaceSpacing", config.support_bottom_interface_spacing);
+    setFloat("supportTopZDistance", config.support_top_z_distance);
+    setFloat("supportBottomZDistance", config.support_bottom_z_distance);
+    setFloat("supportObjectXYDistance", config.support_object_xy_distance);
+    setString("supportBasePattern", config.support_base_pattern);
+    setString("supportInterfacePattern", config.support_interface_pattern);
+    setString("supportStyle", config.support_style);
     setInt("supportFilament", config.support_filament);
     setInt("supportInterfaceFilament", config.support_interface_filament);
     setInt("skirtLoops", config.skirt_loops);
     setFloat("skirtDistance", config.skirt_distance);
     setFloat("brimWidth", config.brim_width);
+    setFloat("outerWallLineWidth", config.outer_wall_line_width);
+    setFloat("innerWallLineWidth", config.inner_wall_line_width);
+    setFloat("topSurfaceLineWidth", config.top_surface_line_width);
+    setFloat("sparseInfillLineWidth", config.sparse_infill_line_width);
+    setFloat("internalSolidInfillLineWidth", config.internal_solid_infill_line_width);
+    setFloat("supportLineWidth", config.support_line_width);
+    setFloat("initialLayerLineWidth", config.initial_layer_line_width);
+    setString("seamPosition", config.seam_position);
+    setString("ironingType", config.ironing_type);
+    setString("ironingPattern", config.ironing_pattern);
+    setFloat("ironingFlow", config.ironing_flow);
+    setFloat("ironingSpacing", config.ironing_spacing);
+    setFloat("ironingSpeed", config.ironing_speed);
+    setString("wallGenerator", config.wall_generator);
+    setString("wallSequence", config.wall_sequence);
+    setBool("detectThinWalls", config.detect_thin_walls);
+    setBool("detectOverhangWalls", config.detect_overhang_walls);
+    setBool("onlyOneWallOnTop", config.only_one_wall_on_top);
+    setBool("preciseOuterWalls", config.precise_outer_walls);
     setFloat("bedSizeX", config.bed_size_x);
     setFloat("bedSizeY", config.bed_size_y);
     setFloat("maxPrintHeight", config.max_print_height);
@@ -271,6 +411,22 @@ jobject configToJava(JNIEnv* env, const SliceConfig& config) {
     setFloat("wipeTowerWidth", config.wipe_tower_width);
     setString("machineStartGcode", config.machine_start_gcode);
     setString("machineEndGcode", config.machine_end_gcode);
+    setString("gcodeFlavor", config.gcode_flavor);
+    setFloat("machineMaxSpeedX", config.machine_max_speed_x);
+    setFloat("machineMaxSpeedY", config.machine_max_speed_y);
+    setFloat("machineMaxSpeedZ", config.machine_max_speed_z);
+    setFloat("machineMaxSpeedE", config.machine_max_speed_e);
+    setFloat("machineMaxAccelerationX", config.machine_max_acceleration_x);
+    setFloat("machineMaxAccelerationY", config.machine_max_acceleration_y);
+    setFloat("machineMaxAccelerationZ", config.machine_max_acceleration_z);
+    setFloat("machineMaxAccelerationE", config.machine_max_acceleration_e);
+    setFloat("machineMaxAccelerationExtruding", config.machine_max_acceleration_extruding);
+    setFloat("machineMaxAccelerationRetracting", config.machine_max_acceleration_retracting);
+    setFloat("machineMaxAccelerationTravel", config.machine_max_acceleration_travel);
+    setFloat("machineMaxJerkX", config.machine_max_jerk_x);
+    setFloat("machineMaxJerkY", config.machine_max_jerk_y);
+    setFloat("machineMaxJerkZ", config.machine_max_jerk_z);
+    setFloat("machineMaxJerkE", config.machine_max_jerk_e);
 
     env->DeleteLocalRef(cls);
     return obj;
diff --git a/app/src/main/cpp/src/sapil_print.cpp b/app/src/main/cpp/src/sapil_print.cpp
index 8d61c9cf..fb5ad330 100644
--- a/app/src/main/cpp/src/sapil_print.cpp
+++ b/app/src/main/cpp/src/sapil_print.cpp
@@ -183,84 +183,110 @@ static void applyConfigToPrusa(Slic3r::DynamicPrintConfig& dpc, const SliceConfi
     dpc.set_key_value("wall_loops", new Slic3r::ConfigOptionInt(config.perimeters));
     dpc.set_key_value("top_shell_layers", new Slic3r::ConfigOptionInt(config.top_solid_layers));
     dpc.set_key_value("bottom_shell_layers", new Slic3r::ConfigOptionInt(config.bottom_solid_layers));
-
-    // Infill (OrcaSlicer keys)
-    Slic3r::InfillPattern pattern = Slic3r::InfillPattern::ipGyroid;
-    if (config.fill_pattern == "grid") pattern = Slic3r::InfillPattern::ipGrid;
-    else if (config.fill_pattern == "honeycomb") pattern = Slic3r::InfillPattern::ipHoneycomb;
-    else if (config.fill_pattern == "line") pattern = Slic3r::InfillPattern::ipLine;
-    else if (config.fill_pattern == "rectilinear") pattern = Slic3r::InfillPattern::ipRectilinear;
-
-    // F87/F91 (2026-05-25): gate fallbacks on !has_embedded_profile so an imported
-    // OrcaSlicer process profile / user-tuned library filament can win. When the Kotlin
-    // ProfileEmbedder has authored the embed (is_snapmaker_profile=true) the embed's
-    // values came from the layered: printer profile + bundled process + library +
-    // SlicingOverrides, so trust it. Raw STL (no embed) still gets these SliceConfig-
-    // derived defaults.
-    if (!has_embedded_profile) {
-        dpc.set_key_value("sparse_infill_pattern", new Slic3r::ConfigOptionEnum<Slic3r::InfillPattern>(pattern));
-        dpc.set_key_value("sparse_infill_density", new Slic3r::ConfigOptionPercent(config.fill_density * 100.0));
-    }
+    dpc.set_key_value("top_shell_thickness", new Slic3r::ConfigOptionFloat(config.top_shell_thickness));
+    dpc.set_key_value("bottom_shell_thickness", new Slic3r::ConfigOptionFloat(config.bottom_shell_thickness));
+
+    // Preserve Orca's complete enum vocabulary. The old hand-written mapping silently
+    // turned unsupported values (notably crosshatch) into gyroid and made distinct
+    // sparse/top/bottom/internal-solid regions look like one generic fill.
+    dpc.set_deserialize_strict("sparse_infill_pattern", config.fill_pattern);
+    dpc.set_deserialize_strict("top_surface_pattern", config.top_surface_pattern);
+    dpc.set_deserialize_strict("bottom_surface_pattern", config.bottom_surface_pattern);
+    dpc.set_deserialize_strict("internal_solid_infill_pattern", config.internal_solid_infill_pattern);
+    dpc.set_key_value("sparse_infill_density", new Slic3r::ConfigOptionPercent(config.fill_density * 100.0));
 
     // Speed (OrcaSlicer keys).
     // outer_wall_speed is the primary speed control — set from the user's "print speed" setting.
-    // All other speeds use profile values (applied from embedded config above) rather than
-    // deriving them all from print_speed.  This gives the profile's per-feature tuning
-    // (inner walls faster, infill faster, bridge slower) while still honouring the user's
-    // overall speed preference via outer_wall_speed.
-    // Fallback values here match standard_0.20mm.json and are used when no profile is embedded.
+    // Preserve feature-specific process tuning instead of deriving the entire profile
+    // from outer-wall speed. SliceOptions has already resolved legacy zero values.
     dpc.set_key_value("outer_wall_speed",          new Slic3r::ConfigOptionFloat(config.print_speed));
-    dpc.set_key_value("inner_wall_speed",          new Slic3r::ConfigOptionFloat(config.print_speed * 1.5));
-    dpc.set_key_value("sparse_infill_speed",       new Slic3r::ConfigOptionFloat(config.print_speed * 1.35));
-    dpc.set_key_value("internal_solid_infill_speed", new Slic3r::ConfigOptionFloat(config.print_speed * 1.25));
-    dpc.set_key_value("top_surface_speed",         new Slic3r::ConfigOptionFloat(config.print_speed));
-    dpc.set_key_value("bridge_speed",              new Slic3r::ConfigOptionFloat(50.0));
-    dpc.set_key_value("gap_infill_speed",          new Slic3r::ConfigOptionFloat(config.print_speed * 1.25));
+    dpc.set_key_value("inner_wall_speed",          new Slic3r::ConfigOptionFloat(config.inner_wall_speed));
+    dpc.set_key_value("sparse_infill_speed",       new Slic3r::ConfigOptionFloat(config.sparse_infill_speed));
+    dpc.set_key_value("internal_solid_infill_speed", new Slic3r::ConfigOptionFloat(config.internal_solid_infill_speed));
+    dpc.set_key_value("top_surface_speed",         new Slic3r::ConfigOptionFloat(config.top_surface_speed));
+    dpc.set_key_value("bridge_speed",              new Slic3r::ConfigOptionFloat(config.bridge_speed));
+    dpc.set_key_value("gap_infill_speed",          new Slic3r::ConfigOptionFloat(config.gap_infill_speed));
     dpc.set_key_value("travel_speed",              new Slic3r::ConfigOptionFloat(config.travel_speed));
     dpc.set_key_value("initial_layer_speed",       new Slic3r::ConfigOptionFloat(config.first_layer_speed));
-    dpc.set_key_value("initial_layer_infill_speed",new Slic3r::ConfigOptionFloat(config.first_layer_speed * 2.1));
-
-    // Snapmaker U1 bed geometry — OrcaSlicer defaults to 200×200mm (Bambu A1).
-    // Must always be set so the slicer knows the actual printable area.
+    dpc.set_key_value("initial_layer_infill_speed",new Slic3r::ConfigOptionFloat(config.first_layer_infill_speed));
+    dpc.set_key_value("support_interface_speed",   new Slic3r::ConfigOptionFloat(config.support_interface_speed));
+    dpc.set_key_value("bridge_flow",               new Slic3r::ConfigOptionFloat(config.bridge_flow_ratio));
+    dpc.set_key_value("internal_bridge_flow",      new Slic3r::ConfigOptionFloat(config.internal_bridge_flow_ratio));
+    dpc.set_key_value("top_solid_infill_flow_ratio", new Slic3r::ConfigOptionFloat(config.top_surface_flow_ratio));
+    dpc.set_key_value("bottom_solid_infill_flow_ratio", new Slic3r::ConfigOptionFloat(config.bottom_surface_flow_ratio));
+
+    // Use the active printer profile geometry. Keep U1 dimensions only as a
+    // defensive fallback for callers using an older Java contract.
+    const double bed_size_x = config.bed_size_x > 0.0f ? config.bed_size_x : 270.0;
+    const double bed_size_y = config.bed_size_y > 0.0f ? config.bed_size_y : 270.0;
+    const double printable_height = config.max_print_height > 0.0f ? config.max_print_height : 270.0;
     dpc.set_key_value("printable_area", new Slic3r::ConfigOptionPoints({
-        {0, 0}, {270, 0}, {270, 270}, {0, 270}
+        {0, 0}, {bed_size_x, 0}, {bed_size_x, bed_size_y}, {0, bed_size_y}
     }));
+    dpc.set_key_value("printable_height", new Slic3r::ConfigOptionFloat(printable_height));
 
     // Bed plate type — OrcaSlicer defaults to btPC (Engineering Plate) whose temp keys
     // are cool_plate_temp (35°C for PLA).  The Snapmaker U1 uses a textured PEI plate,
     // so set btPEI so OrcaSlicer resolves bed temp from hot_plate_temp (60°C for PLA).
     dpc.set_key_value("curr_bed_type", new Slic3r::ConfigOptionEnum<Slic3r::BedType>(Slic3r::btPEI));
 
-    // Snapmaker U1 machine kinematic limits — fallback values used when no Snapmaker
-    // profile is embedded (e.g. plain STL files).  For 3MF files these are overridden
-    // by the values from the embedded profile in the is_snapmaker_profile block above.
-    // Values match snapmaker_u1.json: {normal, silent} mode pairs.
-    dpc.set_key_value("machine_max_speed_x",          new Slic3r::ConfigOptionFloats({500.0, 200.0}));
-    dpc.set_key_value("machine_max_speed_y",          new Slic3r::ConfigOptionFloats({500.0, 200.0}));
-    dpc.set_key_value("machine_max_speed_z",          new Slic3r::ConfigOptionFloats({20.0, 12.0}));
-    dpc.set_key_value("machine_max_speed_e",          new Slic3r::ConfigOptionFloats({30.0, 25.0}));
-    dpc.set_key_value("machine_max_acceleration_x",   new Slic3r::ConfigOptionFloats({20000.0, 20000.0}));
-    dpc.set_key_value("machine_max_acceleration_y",   new Slic3r::ConfigOptionFloats({20000.0, 20000.0}));
-    dpc.set_key_value("machine_max_acceleration_z",   new Slic3r::ConfigOptionFloats({500.0, 200.0}));
-    dpc.set_key_value("machine_max_acceleration_e",   new Slic3r::ConfigOptionFloats({5000.0, 5000.0}));
-    dpc.set_key_value("machine_max_jerk_x",           new Slic3r::ConfigOptionFloats({9.0, 9.0}));
-    dpc.set_key_value("machine_max_jerk_y",           new Slic3r::ConfigOptionFloats({9.0, 9.0}));
-    dpc.set_key_value("machine_max_jerk_z",           new Slic3r::ConfigOptionFloats({3.0, 0.4}));
-    dpc.set_key_value("machine_max_jerk_e",           new Slic3r::ConfigOptionFloats({2.5, 2.5}));
-    // M204 acceleration values — must match U1 profile for correct G-code and time estimation.
-    // OrcaSlicer defaults (1500/1250 mm/s²) are 13× too low and inflate print time estimates.
-    dpc.set_key_value("machine_max_acceleration_extruding",  new Slic3r::ConfigOptionFloats({20000.0, 20000.0}));
-    dpc.set_key_value("machine_max_acceleration_retracting", new Slic3r::ConfigOptionFloats({5000.0, 5000.0}));
-    dpc.set_key_value("machine_max_acceleration_travel",     new Slic3r::ConfigOptionFloats({20000.0, 20000.0}));
-    // Per-feature accelerations from the standard process profile.
-    // Without these OrcaSlicer uses FullPrintConfig::defaults() which are very low,
-    // making every short move appear to be in the acceleration ramp → 5× time inflation.
-    dpc.set_key_value("default_acceleration",          new Slic3r::ConfigOptionFloat(10000.0));
-    dpc.set_key_value("outer_wall_acceleration",       new Slic3r::ConfigOptionFloat(5000.0));
-    dpc.set_key_value("inner_wall_acceleration",       new Slic3r::ConfigOptionFloat(0.0));
-    dpc.set_key_value("top_surface_acceleration",      new Slic3r::ConfigOptionFloat(2000.0));
-    dpc.set_key_value("travel_acceleration",           new Slic3r::ConfigOptionFloat(10000.0));
-    dpc.set_key_value("initial_layer_acceleration",    new Slic3r::ConfigOptionFloat(500.0));
+    // Machine limits come from the active printer profile. Store two equal modes
+    // because this mobile contract does not currently expose Orca's silent mode.
+    auto positive = [](float value, double fallback) {
+        return value > 0.0f ? static_cast<double>(value) : fallback;
+    };
+    auto machine_modes = [](double value) {
+        return new Slic3r::ConfigOptionFloats({value, value});
+    };
+    const double max_speed_x = positive(config.machine_max_speed_x, 500.0);
+    const double max_speed_y = positive(config.machine_max_speed_y, 500.0);
+    const double max_acceleration_x = positive(config.machine_max_acceleration_x, 20000.0);
+    const double max_acceleration_y = positive(config.machine_max_acceleration_y, 20000.0);
+    const double max_acceleration_extruding = positive(config.machine_max_acceleration_extruding, 20000.0);
+    const double max_acceleration_travel = positive(config.machine_max_acceleration_travel, 20000.0);
+    dpc.set_key_value("machine_max_speed_x", machine_modes(max_speed_x));
+    dpc.set_key_value("machine_max_speed_y", machine_modes(max_speed_y));
+    dpc.set_key_value("machine_max_speed_z", machine_modes(positive(config.machine_max_speed_z, 20.0)));
+    dpc.set_key_value("machine_max_speed_e", machine_modes(positive(config.machine_max_speed_e, 30.0)));
+    dpc.set_key_value("machine_max_acceleration_x", machine_modes(max_acceleration_x));
+    dpc.set_key_value("machine_max_acceleration_y", machine_modes(max_acceleration_y));
+    dpc.set_key_value("machine_max_acceleration_z", machine_modes(positive(config.machine_max_acceleration_z, 500.0)));
+    dpc.set_key_value("machine_max_acceleration_e", machine_modes(positive(config.machine_max_acceleration_e, 5000.0)));
+    dpc.set_key_value("machine_max_jerk_x", machine_modes(positive(config.machine_max_jerk_x, 9.0)));
+    dpc.set_key_value("machine_max_jerk_y", machine_modes(positive(config.machine_max_jerk_y, 9.0)));
+    dpc.set_key_value("machine_max_jerk_z", machine_modes(positive(config.machine_max_jerk_z, 3.0)));
+    dpc.set_key_value("machine_max_jerk_e", machine_modes(positive(config.machine_max_jerk_e, 2.5)));
+    dpc.set_key_value("machine_max_acceleration_extruding", machine_modes(max_acceleration_extruding));
+    dpc.set_key_value("machine_max_acceleration_retracting", machine_modes(positive(config.machine_max_acceleration_retracting, 5000.0)));
+    dpc.set_key_value("machine_max_acceleration_travel", machine_modes(max_acceleration_travel));
+
+    Slic3r::GCodeFlavor flavor = Slic3r::gcfMarlinLegacy;
+    if (config.gcode_flavor == "marlin2") flavor = Slic3r::gcfMarlinFirmware;
+    else if (config.gcode_flavor == "klipper") flavor = Slic3r::gcfKlipper;
+    dpc.set_key_value("gcode_flavor", new Slic3r::ConfigOptionEnum<Slic3r::GCodeFlavor>(flavor));
+
+    const double xy_machine_acceleration = std::min(max_acceleration_x, max_acceleration_y);
+    const double default_acceleration = config.default_acceleration > 0.0f
+        ? static_cast<double>(config.default_acceleration)
+        : std::min(max_acceleration_extruding, xy_machine_acceleration);
+    const double outer_wall_acceleration = config.outer_wall_acceleration > 0.0f
+        ? static_cast<double>(config.outer_wall_acceleration)
+        : default_acceleration * 0.5;
+    const double top_surface_acceleration = config.top_surface_acceleration > 0.0f
+        ? static_cast<double>(config.top_surface_acceleration)
+        : default_acceleration * 0.2;
+    const double travel_acceleration = config.travel_acceleration > 0.0f
+        ? static_cast<double>(config.travel_acceleration)
+        : std::min(max_acceleration_travel, xy_machine_acceleration);
+    const double first_layer_acceleration = config.first_layer_acceleration > 0.0f
+        ? static_cast<double>(config.first_layer_acceleration)
+        : std::min(500.0, default_acceleration);
+    dpc.set_key_value("default_acceleration",          new Slic3r::ConfigOptionFloat(default_acceleration));
+    dpc.set_key_value("outer_wall_acceleration",       new Slic3r::ConfigOptionFloat(outer_wall_acceleration));
+    dpc.set_key_value("inner_wall_acceleration",       new Slic3r::ConfigOptionFloat(config.inner_wall_acceleration));
+    dpc.set_key_value("top_surface_acceleration",      new Slic3r::ConfigOptionFloat(top_surface_acceleration));
+    dpc.set_key_value("travel_acceleration",           new Slic3r::ConfigOptionFloat(travel_acceleration));
+    dpc.set_key_value("initial_layer_acceleration",    new Slic3r::ConfigOptionFloat(first_layer_acceleration));
 
     // Multi-extruder setup
     int n_ext = std::max(1, config.extruder_count);
@@ -461,11 +487,9 @@ static void applyConfigToPrusa(Slic3r::DynamicPrintConfig& dpc, const SliceConfi
     applyPerExtFloats("slow_down_layer_time",    config.filament_slow_down_layer_times, 4.0);
     applyPerExtFloats("slow_down_min_speed",     config.filament_slow_down_min_speeds, 20.0);
 
-    // G-code dialect — NOT set as a fallback here because gcode_flavor=klipper suppresses
-    // M104/M109 temp commands from the slicer body, relying on the start G-code template.
-    // For raw STL files (no embedded profile), the Marlin default is correct.
-    // When the Snapmaker profile IS embedded, gcode_flavor=klipper is applied from the
-    // profile_keys[] whitelist in the is_snapmaker_profile block above.
+    // G-code flavor is set from the active printer profile above. A Klipper
+    // profile must provide a compatible start template because Orca delegates
+    // part of temperature setup to that template for this flavor.
 
     // Deretraction speed — OrcaSlicer defaults to 0 (= same as retraction speed).
     // Snapmaker profile uses 35 mm/s for smoother prime-after-retract.
@@ -480,29 +504,53 @@ static void applyConfigToPrusa(Slic3r::DynamicPrintConfig& dpc, const SliceConfi
     // the literal 0, not the auto-resolved value, causing Flow::rounded_rectangle_extrusion_spacing()
     // to throw FlowErrorNegativeSpacing on multi-color models.
     double nd = config.nozzle_diameter;
+    const double outer_wall_width = config.outer_wall_line_width > 0.0f
+        ? config.outer_wall_line_width : nd * 1.05;
+    const double inner_wall_width = config.inner_wall_line_width > 0.0f
+        ? config.inner_wall_line_width : nd * 1.125;
+    const double top_surface_width = config.top_surface_line_width > 0.0f
+        ? config.top_surface_line_width : nd * 1.05;
+    const double sparse_infill_width = config.sparse_infill_line_width > 0.0f
+        ? config.sparse_infill_line_width : nd * 1.125;
+    const double internal_solid_infill_width = config.internal_solid_infill_line_width > 0.0f
+        ? config.internal_solid_infill_line_width : nd * 1.125;
+    const double support_width = config.support_line_width > 0.0f
+        ? config.support_line_width : nd * 1.05;
+    const double initial_layer_width = config.initial_layer_line_width > 0.0f
+        ? config.initial_layer_line_width : nd * 1.25;
     dpc.set_key_value("line_width",              new Slic3r::ConfigOptionFloatOrPercent(nd * 1.125, false));
-    dpc.set_key_value("outer_wall_line_width",   new Slic3r::ConfigOptionFloatOrPercent(nd * 1.05,  false));
-    dpc.set_key_value("inner_wall_line_width",   new Slic3r::ConfigOptionFloatOrPercent(nd * 1.125, false));
-    dpc.set_key_value("top_surface_line_width",  new Slic3r::ConfigOptionFloatOrPercent(nd * 1.05,  false));
-    dpc.set_key_value("sparse_infill_line_width",new Slic3r::ConfigOptionFloatOrPercent(nd * 1.125, false));
-    dpc.set_key_value("initial_layer_line_width",new Slic3r::ConfigOptionFloatOrPercent(nd * 1.25,  false));
-
-    // Wall generator: keep clamped to Arachne for everyone.
-    // OrcaSlicer 2.2.4's classic perimeter generator crashes (SIGSEGV in
-    // PerimeterGenerator::process_classic / MultiPoint dtor) on the U1 process config
-    // — surfaced 2026-05-25 when F87 first un-gated this key. Pre-existing OrcaSlicer
-    // issue; until it's fixed upstream we must not let the user reach the classic path.
-    dpc.set_key_value("wall_generator", new Slic3r::ConfigOptionEnum<Slic3r::PerimeterGeneratorType>(Slic3r::PerimeterGeneratorType::Arachne));
-
-    // F87 (2026-05-25): gate seam_position + reduce_infill_retraction on
-    // !has_embedded_profile so an imported process profile or user SlicingOverride
-    // can win. `buildProfileOverrides` already writes the resolved values into the
-    // embed map for Snapmaker-authored embeds.
+    dpc.set_key_value("outer_wall_line_width",   new Slic3r::ConfigOptionFloatOrPercent(outer_wall_width, false));
+    dpc.set_key_value("inner_wall_line_width",   new Slic3r::ConfigOptionFloatOrPercent(inner_wall_width, false));
+    dpc.set_key_value("top_surface_line_width",  new Slic3r::ConfigOptionFloatOrPercent(top_surface_width, false));
+    dpc.set_key_value("sparse_infill_line_width",new Slic3r::ConfigOptionFloatOrPercent(sparse_infill_width, false));
+    dpc.set_key_value("internal_solid_infill_line_width", new Slic3r::ConfigOptionFloatOrPercent(internal_solid_infill_width, false));
+    dpc.set_key_value("support_line_width",      new Slic3r::ConfigOptionFloatOrPercent(support_width, false));
+    dpc.set_key_value("initial_layer_line_width",new Slic3r::ConfigOptionFloatOrPercent(initial_layer_width, false));
+
+    const Slic3r::PerimeterGeneratorType wall_generator = config.wall_generator == "classic"
+        ? Slic3r::PerimeterGeneratorType::Classic
+        : Slic3r::PerimeterGeneratorType::Arachne;
+    dpc.set_key_value("wall_generator", new Slic3r::ConfigOptionEnum<Slic3r::PerimeterGeneratorType>(wall_generator));
+
+    Slic3r::WallSequence wall_sequence = Slic3r::WallSequence::InnerOuter;
+    if (config.wall_sequence == "outer-inner") {
+        wall_sequence = Slic3r::WallSequence::OuterInner;
+    } else if (config.wall_sequence == "inner-outer-inner") {
+        wall_sequence = Slic3r::WallSequence::InnerOuterInner;
+    }
+    dpc.set_key_value("wall_sequence", new Slic3r::ConfigOptionEnum<Slic3r::WallSequence>(wall_sequence));
+    dpc.set_key_value("detect_thin_wall", new Slic3r::ConfigOptionBool(config.detect_thin_walls));
+    dpc.set_key_value("detect_overhang_wall", new Slic3r::ConfigOptionBool(config.detect_overhang_walls));
+    dpc.set_key_value("only_one_wall_top", new Slic3r::ConfigOptionBool(config.only_one_wall_on_top));
+    dpc.set_key_value("precise_outer_wall", new Slic3r::ConfigOptionBool(config.precise_outer_walls));
+
+    dpc.set_deserialize_strict("seam_position", config.seam_position);
+    dpc.set_deserialize_strict("ironing_type", config.ironing_type);
+    dpc.set_deserialize_strict("ironing_pattern", config.ironing_pattern);
+    dpc.set_key_value("ironing_flow", new Slic3r::ConfigOptionPercent(config.ironing_flow));
+    dpc.set_key_value("ironing_spacing", new Slic3r::ConfigOptionFloat(config.ironing_spacing));
+    dpc.set_key_value("ironing_speed", new Slic3r::ConfigOptionFloat(config.ironing_speed));
     if (!has_embedded_profile) {
-        // Seam position - aligned is the default; set explicitly for STL files.
-        dpc.set_key_value("seam_position", new Slic3r::ConfigOptionEnum<Slic3r::SeamPosition>(Slic3r::spAligned));
-
-        // Reduce infill retraction - off by default for Snapmaker U1.
         dpc.set_key_value("reduce_infill_retraction", new Slic3r::ConfigOptionBool(false));
     }
 
@@ -568,19 +616,27 @@ static void applyConfigToPrusa(Slic3r::DynamicPrintConfig& dpc, const SliceConfi
         dpc.set_key_value("filament_cost", new Slic3r::ConfigOptionFloats(std::move(v)));
     }
 
-    // Support speeds — OrcaSlicer defaults to 80mm/s which is fine today, but pin to
-    // Snapmaker profile values so future OrcaSlicer changes don't silently regress.
-    dpc.set_key_value("support_speed", new Slic3r::ConfigOptionFloat(100.0));
-    dpc.set_key_value("support_interface_speed", new Slic3r::ConfigOptionFloat(80.0));
-
-    // Overhang speed control — OrcaSlicer defaults enable_overhang_speed=true with all
-    // overhang speeds at 0 (= use wall speed).  Pin to Snapmaker 0.4mm nozzle profile
-    // values so a future default change doesn't break overhang quality.
-    dpc.set_key_value("enable_overhang_speed", new Slic3r::ConfigOptionBool(true));
-    dpc.set_key_value("overhang_1_4_speed", new Slic3r::ConfigOptionFloatOrPercent(55, false));
-    dpc.set_key_value("overhang_2_4_speed", new Slic3r::ConfigOptionFloatOrPercent(30, false));
-    dpc.set_key_value("overhang_3_4_speed", new Slic3r::ConfigOptionFloatOrPercent(10, false));
-    dpc.set_key_value("overhang_4_4_speed", new Slic3r::ConfigOptionFloatOrPercent(10, false));
+    // Preserve the active process profile's support speed.
+    dpc.set_key_value("support_speed", new Slic3r::ConfigOptionFloat(config.support_speed));
+    dpc.set_key_value("support_interface_speed", new Slic3r::ConfigOptionFloat(config.support_interface_speed));
+    dpc.set_key_value("support_interface_top_layers", new Slic3r::ConfigOptionInt(config.support_interface_top_layers));
+    dpc.set_key_value("support_interface_bottom_layers", new Slic3r::ConfigOptionInt(config.support_interface_bottom_layers));
+    dpc.set_key_value("support_interface_spacing", new Slic3r::ConfigOptionFloat(config.support_interface_spacing));
+    dpc.set_key_value("support_bottom_interface_spacing", new Slic3r::ConfigOptionFloat(config.support_bottom_interface_spacing));
+    dpc.set_key_value("support_top_z_distance", new Slic3r::ConfigOptionFloat(config.support_top_z_distance));
+    dpc.set_key_value("support_bottom_z_distance", new Slic3r::ConfigOptionFloat(config.support_bottom_z_distance));
+    dpc.set_key_value("support_object_xy_distance", new Slic3r::ConfigOptionFloat(config.support_object_xy_distance));
+    dpc.set_deserialize_strict("support_base_pattern", config.support_base_pattern);
+    dpc.set_deserialize_strict("support_interface_pattern", config.support_interface_pattern);
+    dpc.set_deserialize_strict("support_style", config.support_style);
+
+    // Orca profiles intentionally mix absolute mm/s and percentages of outer-wall
+    // speed. Preserve both the numeric value and its unit semantics per stage.
+    dpc.set_key_value("enable_overhang_speed", new Slic3r::ConfigOptionBool(config.overhang_speed_enabled));
+    dpc.set_key_value("overhang_1_4_speed", new Slic3r::ConfigOptionFloatOrPercent(config.overhang_speed_1, config.overhang_speed_1_percent));
+    dpc.set_key_value("overhang_2_4_speed", new Slic3r::ConfigOptionFloatOrPercent(config.overhang_speed_2, config.overhang_speed_2_percent));
+    dpc.set_key_value("overhang_3_4_speed", new Slic3r::ConfigOptionFloatOrPercent(config.overhang_speed_3, config.overhang_speed_3_percent));
+    dpc.set_key_value("overhang_4_4_speed", new Slic3r::ConfigOptionFloatOrPercent(config.overhang_speed_4, config.overhang_speed_4_percent));
 
     // Idle extruder standby temperature — prevents idle nozzles from staying at full print temp.
     // Snapmaker profiles embed ooze_prevention=0 and standby_temperature_delta=-5, which leaves
diff --git a/app/src/main/cpp/orcaslicer/src/libslic3r/PerimeterGenerator.cpp b/app/src/main/cpp/orcaslicer/src/libslic3r/PerimeterGenerator.cpp
index 4e0ddd0f3f..4e8e2b2ef3 100644
--- a/app/src/main/cpp/orcaslicer/src/libslic3r/PerimeterGenerator.cpp
+++ b/app/src/main/cpp/orcaslicer/src/libslic3r/PerimeterGenerator.cpp
@@ -103,12 +103,14 @@ static ExtrusionEntityCollection traverse_loops(const PerimeterGenerator &perime
     // loops is an arrayref of ::Loop objects
     // turn each one into an ExtrusionLoop object
     ExtrusionEntityCollection   coll;
+    std::vector<size_t>         loop_entity_indices;
     
     // Detect steep overhangs
     bool overhangs_reverse = perimeter_generator.config->overhang_reverse &&
                              perimeter_generator.layer_id % 2 == 1; // Only calculate overhang degree on even (from GUI POV) layers
 
-    for (const PerimeterGeneratorLoop &loop : loops) {
+    for (size_t loop_idx = 0; loop_idx < loops.size(); ++loop_idx) {
+        const PerimeterGeneratorLoop &loop = loops[loop_idx];
         bool is_external = loop.is_external();
         bool is_small_width = loop.is_smaller_width_perimeter;
         
@@ -224,6 +226,7 @@ static ExtrusionEntityCollection traverse_loops(const PerimeterGenerator &perime
         }
 
         coll.append(ExtrusionLoop(std::move(paths), loop_role));
+        loop_entity_indices.push_back(loop_idx);
     }
     
     // Append thin walls to the nearest-neighbor search (only for first iteration)
@@ -233,20 +236,20 @@ static ExtrusionEntityCollection traverse_loops(const PerimeterGenerator &perime
     }
     
     // Traverse children and build the final collection.
-	Point zero_point(0, 0);
-	std::vector<std::pair<size_t, bool>> chain = chain_extrusion_entities(coll.entities, &zero_point);
+    Point zero_point(0, 0);
+    std::vector<std::pair<size_t, bool>> chain = chain_extrusion_entities(coll.entities, &zero_point);
     ExtrusionEntityCollection out;
     for (const std::pair<size_t, bool> &idx : chain) {
-		assert(coll.entities[idx.first] != nullptr);
-        if (idx.first >= loops.size()) {
+        assert(coll.entities[idx.first] != nullptr);
+        if (idx.first >= loop_entity_indices.size()) {
             // This is a thin wall.
-			out.entities.reserve(out.entities.size() + 1);
+            out.entities.reserve(out.entities.size() + 1);
             out.entities.emplace_back(coll.entities[idx.first]);
-			coll.entities[idx.first] = nullptr;
+            coll.entities[idx.first] = nullptr;
             if (idx.second)
-				out.entities.back()->reverse();
+                out.entities.back()->reverse();
         } else {
-            const PerimeterGeneratorLoop &loop = loops[idx.first];
+            const PerimeterGeneratorLoop &loop = loops[loop_entity_indices[idx.first]];
             assert(thin_walls.empty());
             ExtrusionEntityCollection children = traverse_loops(perimeter_generator, loop.children, thin_walls, steep_overhang_contour, steep_overhang_hole);
             out.entities.reserve(out.entities.size() + children.entities.size() + 1);
